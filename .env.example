# Campus IT Service Desk Triage Agent
# Native Salesforce DX project. No API keys, OAuth secrets, or Connected Apps.
#
# Do not add credentials here. Authenticate the Salesforce CLI instead:
#   sf org login web --alias campus-it-dev --set-default
#
# Optional local aliases (never commit the real values):
# SF_TARGET_ORG=campus-it-dev
